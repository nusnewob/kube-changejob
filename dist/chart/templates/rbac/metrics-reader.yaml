{{- if .Values.metrics.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    name: kube-changejob-metrics-reader
rules:
    - nonResourceURLs:
        - /metrics
      verbs:
        - get
{{- end }}
