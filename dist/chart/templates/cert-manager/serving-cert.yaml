{{- if .Values.certManager.enable }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: kube-changejob
    name: kube-changejob-serving-cert
    namespace: {{ .Release.Namespace }}
spec:
    dnsNames:
        - kube-changejob-webhook-service.{{ .Release.Namespace }}.svc
        - kube-changejob-webhook-service.{{ .Release.Namespace }}.svc.cluster.local
    issuerRef:
        kind: Issuer
        name: {{ include "chart.name" . }}-selfsigned-issuer
    secretName: webhook-server-cert
{{- end }}
