flag_management:
  individual_flags:
    - name: controller
      paths:
        - internal/
    - name: cmd
      paths:
        - cmd/
component_management:
  individual_components:
    - name: controller
      component_id: controller
      paths:
        - internal/
    - name: cmd
      component_id: cmd
      paths:
        - cmd/
